name: Tests

on:
  push:
    branches:
      - master
    tags:
      - "v*"
  pull_request:

jobs:
  tests:
    name: Python ${{ matrix.python }} - on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        python: [3.6, 3.7, 3.8]
        os: [ubuntu-latest, macos-latest, windows-latest]

        include:
          # mark default
          - do-coverage: false

          # mark case for coverage reporting
          - python: 3.7
            os: ubuntu-latest
            do-coverage: true

    steps:
      - name: Select test for coverage
        run: |
          echo "Python ${{ matrix.python }} is 3.7 ${{ matrix.python == 3.7 }}"
          echo "os ${{ matrix.os }} is ubuntu-latest ${{ matrix.os == 'ubuntu-latest' }}"
          echo "${{ matrix.do-coverage }}"
      - name: Checkout code
        uses: actions/checkout@v2.3.4
        with:
          fetch-depth: 0
      - name: Setup Python
        uses: actions/setup-python@v2.2.2
        with:
          python-version: ${{ matrix.python }}
      - name: Install dependencies
        run: python -m pip install -r requirements.txt
        shell: bash
      - name: Run test
        run: |
          if ${{ matrix.do-coverage }}; then
            coverage run -m unittest discover
          else
            python -m unittest discover
          fi
        shell: bash
      - name: Report coverage
        if: ${{ matrix.do-coverage && !failure() }}
        uses: codecov/codecov-action@v1
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: true
